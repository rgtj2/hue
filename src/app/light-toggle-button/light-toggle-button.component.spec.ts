/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { LightToggleButtonComponent } from './light-toggle-button.component';

describe('Component: LightToggleButton', () => {
  it('should create an instance', () => {
    let component = new LightToggleButtonComponent();
    expect(component).toBeTruthy();
  });
});
